export enum TooltipPosition {
  ABOVE = 'above',
  BELOW = 'below',
  LEFT = 'left',
  RIGHT = 'right',
  DEFAULT = 'above'
}

// feel free to add more buttons and use while using directive :)
// Recommended use official names of button to create logic with them in future updates of app
export enum TooltipHotkey {
  META = '⌘',
  CONTROL = 'Control',
  ALT = 'Alt',
  SLASH = '/',
  BACK_SLASH = '\\'
}
